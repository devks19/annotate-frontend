# version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://98.130.112.17:8088/api
    container_name: annotate-frontend
    ports:
      - "80:5173"
    restart: unless-stopped