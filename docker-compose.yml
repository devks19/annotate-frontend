# version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://18.61.22.154:80
    container_name: annotate-frontend
    ports:
      - "80:80"
    restart: unless-stopped